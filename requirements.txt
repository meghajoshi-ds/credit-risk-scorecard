# Runtime dependencies — what app.py needs to score and explain.
# Kept minimal so the hosted Streamlit deploy builds quickly and reliably.
joblib==1.6.0
lightgbm==4.7.0
numpy==2.5.3
pandas==3.0.6
scikit-learn==1.9.1
scipy==1.18.1
shap==0.52.0
streamlit==1.64.0
