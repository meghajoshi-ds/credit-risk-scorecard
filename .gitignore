venv/
.DS_Store
__pycache__/
.ipynb_checkpoints/

# Keep the raw source (17MB) so the repo is runnable after a clone.
# Exclude the cleaned file — notebook 02 regenerates it in seconds.
data/lending_club_clean.csv

# Model files ARE committed (~680KB total). They are small, and committing them
# is what lets `app.py` run from a fresh clone - required for the hosted demo,
# since Streamlit Community Cloud only has the repo, not a local notebook run.

# Local preview only; GitHub renders README.md directly.
outputs/html/README.html
outputs/html/INTERVIEW_PREP.html
